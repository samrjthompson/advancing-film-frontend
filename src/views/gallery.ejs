<%layout('layouts/boilerplate')%>

<div class="container">

    <div class="container">
        <div class="gallery">
            <% for (var i = 0; i < imageUrls.length; i++) { %>
                <img src="<%= imageUrls[i] %>" alt="Gallery image <%= i %>">
            <% } %>
        </div>
    </div>

</div>